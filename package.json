{
	"name": "aglio",
	"version": "1.0.0",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*",
		"tests/*"
	],
	"scripts": {
		"dev": "turbo run dev",
		"gen": "turbo run gen",
		"build": "turbo run build",
		"preview": "turbo run preview",
		"dev:storage": "turbo run dev --filter=storage-server --filter=storage --filter=storage-common --filter=storage-react"
	},
	"repository": "https://github.com/a-type/aglio",
	"author": "Grant Forrest <gaforres@gmail.com>",
	"devDependencies": {
		"@types/node": "16.11.7",
		"turbo": "^1.3.1",
		"typescript": "^4.7.4"
	}
}
